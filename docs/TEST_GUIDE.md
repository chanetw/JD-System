# คู่มือการทดสอบระบบ (System Testing Guide) 🧪

เอกสารฉบับนี้รวบรวมข้อมูลที่จำเป็นสำหรับการทดสอบระบบ DJ System ทั้งหมด รวมถึงข้อมูลผู้ใช้ทดสอบ แผนการดำเนินงาน และรายการงานที่ต้องทำ

---

## 🔐 ข้อมูลผู้ใช้สำหรับทดสอบ (Test Credentials)

ผู้ใช้เหล่านี้ถูกสร้างขึ้นใน Database เรียบร้อยแล้วเพื่อใช้ทดสอบการเข้าถึงตามบทบาท (RBAC)

| บทบาท (Role) | อีเมล (Email) | รหัสผ่าน (Password) | สิทธิ์การเข้าถึง |
| :--- | :--- | :--- | :--- |
| 👑 **Admin** | `admin@test.com` | `Test@123` | เข้าถึงได้ทุกเมนู, จัดการข้อมูล Master, อนุมัติงานทุกระดับ |
| 👔 **Manager** | `manager@test.com` | `Test@123` | อนุมัติงาน, มอบหมายงาน, ดู Dashboard ภาพรวม |
| 🎨 **Designer** | `designer@test.com` | `Test@123` | ดำเนินการรับงาน-ส่งงาน, ดูคิวงานของตนเอง |
| 📝 **Requester** | `requester@test.com` | `Test@123` | สร้างงานใหม่ (DJ Request), ติดตามงาน, ยืนยันปิดงาน |

---

## 📋 แผนการดำเนินงาน (Implementation Plan Summary)

### เป้าหมายหลัก
1.  **Smart Delete**: ปรับปรุงระบบลบข้อมูล (Tenant, Department, BUD) ให้ฉลาดขึ้น โดยใช้ Hard Delete เมื่อไม่มีข้อมูลผูกพัน และ Soft Delete (Inactive) เมื่อมีข้อมูลใช้งานอยู่
2.  **UI Fix**: แก้ไขบั๊กการแสดงผลในหน้า Job Detail ส่วน Preview โดยจัดพื้นหลังสีดำและแก้ปัญหา Icon ทับเส้นขอบ
3.  **RBAC Testing**: จัดเตรียมสภาพแวดล้อมและแผนการทดสอบตามบทบาทผู้ใช้ (Role-Based Access Control)

### รายละเอียดการเปลี่ยนแปลง
*   **Backend**: 
    *   แก้ไข `tenants.js`, `departments.js`, `buds.js` เพื่อรองรับตรรกะ Smart Delete
    *   เพิ่มสคริปต์ `create-test-users.js` สำหรับสร้างผู้ใช้ทดสอบอัตโนมัติ
*   **Frontend**: 
    *   แก้ไข `JobDetail.jsx` ในส่วนของ Preview Layout
    *   ปรับปรุง `OrganizationManagement.jsx` ให้กรองข้อมูลที่ Inactive ออกจากรายการ

---

## ✅ รายการงานที่ดำเนินการ (Task Status)

| หมวดหมู่ | งาน (Task) | สถานะ |
| :--- | :--- | :--- |
| **Organization** | Implement Smart Tenant/Dept Delete | ✅ เสร็จสิ้น |
| **Logic Fix** | แก้ไข BUD Creation `tenantId` Logic | ✅ เสร็จสิ้น |
| **UI Fix** | แก้ไข Preview Area ในหน้า Job Detail (พื้นหลังดำ/ไอคอนทับขอบ) | ✅ เสร็จสิ้น |
| **Testing Prep** | สร้างผู้ใช้ทดสอบ 4 Roles (Admin, Manager, Designer, Requester) | ✅ เสร็จสิ้น |
| **Testing Prep** | แผนการทดสอบระบบตาม Role (52 Test Cases) | ✅ เสร็จสิ้น |
| **Workflow** | สร้างข้อมูลตัวอย่าง (งาน/สถานะต่างๆ) | ⏳ รอดำเนินการ |
| **E2E Test** | ทดสอบ End-to-End Workflow ครบวงจร | ⏳ รอดำเนินการ |

---

## 📝 แผนการทดสอบแนะนำ (Recommended Test Scenarios)

1.  **Workflow พื้นฐาน**:
    *   Requester สร้างงาน -> Manager อนุมัติ -> Admin มอบหมาย -> Designer ส่งงาน -> Requester ปิดงาน
2.  **Smart Delete**:
    *   ลองลบ Tenant ที่ไม่มี BUD (ต้องหายไปจากระบบ)
    *   ลองลบ Tenant ที่มี BUD อยู่ (สถานะต้องกลายเป็น Inactive และไม่ปรากฏใน Dropdown ตอนสร้างงาน)
3.  **UI Layout**:
    *   ตรวจสอบหน้า Job Detail ว่าส่วน Preview แสดงผลสวยงาม ไม่ทับเส้นขอบ

---

> [!NOTE]
> เอกสารฉบับนี้ถูกรวบรวมและสร้างขึ้นมาเพื่ออำนวยความสะดวกในการทดสอบล่าสุดที่ดำเนินการแก้ไขไป
