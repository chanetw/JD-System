# คู่มือ Deploy ระบบ User Registration Approval Workflow
**วันที่:** 30 มกราคม 2026
**สถานะ:** ✅ พร้อม Deploy (เหลืออีก 2 ขั้นตอน)

---

## ✅ งานที่เสร็จสมบูรณ์แล้ว (100%)

### Backend Implementation
- ✅ PrismaV1Adapter - ระบบ generate password อัตโนมัติ (12 ตัวอักษร)
- ✅ API Endpoints ครบทั้ง 6 endpoints
  - `POST /api/v2/auth/register-request` (สมัครใช้งานไม่ต้องใส่ password)
  - `POST /api/v2/auth/approve-registration` (admin อนุมัติ)
  - `POST /api/v2/auth/reject-registration` (admin ปฏิเสธ)
  - `POST /api/v2/auth/change-password` (เปลี่ยน password)
  - `POST /api/v2/auth/login` (login)
  - `POST /api/v2/auth/verify-token` (verify token)
- ✅ EmailService - ส่งเมล์พร้อม password อัตโนมัติ
  - Email template สวยงาม (HTML + Plain Text)
  - ภาษาไทยทั้งหมด
  - Fallback mechanism (แสดง password ใน modal ถ้าส่งเมล์ไม่สำเร็จ)
- ✅ Database Schema - เพิ่ม field `must_change_password`

### Frontend Implementation
- ✅ Register.jsx (V1 UI ภาษาไทย) - ลบช่องใส่ password ออก
- ✅ Login.tsx (V2) - detect mustChangePassword และ redirect
- ✅ ForceChangePassword.tsx - หน้าบังคับเปลี่ยน password
  - Password strength indicator
  - Validation ครบถ้วน
- ✅ PendingApprovals.tsx - แสดงสถานะการส่งเมล์
- ✅ App.jsx - เพิ่ม route `/force-change-password`
- ✅ TypeScript types - เพิ่ม `mustChangePassword` field

### Database
- ✅ Migration 015 ถูกรันแล้ว - เพิ่ม column `must_change_password`

### Documentation
- ✅ DEPLOYMENT_REPORT_2026-01-30.md (รายงานฉบับเต็ม)
- ✅ DEPLOYMENT_CHECKLIST_QUICK.md (checklist สั้น)
- ✅ API_ENDPOINTS_SUMMARY.md (เอกสาร API)
- ✅ USER_REGISTRATION_APPROVAL_WORKFLOW.md (คู่มือ workflow)

---

## ⚠️ ขั้นตอนสุดท้ายก่อน Deploy (เหลือ 2 ขั้นตอน)

### 🔧 ขั้นตอนที่ 1: Regenerate Prisma Client

**ทำที่:** Backend API Server

```bash
# เปิด Terminal
cd backend/api-server

# รัน generate
npx prisma generate

# ควรเห็นข้อความ
# ✔ Generated Prisma Client (v5.x.x) to ./node_modules/@prisma/client
```

**ทำไมต้องรัน:**
- เพิ่ม TypeScript types สำหรับ field `mustChangePassword`
- ให้ Prisma Client รู้จัก column ใหม่
- ทำให้ autocomplete และ type checking ทำงาน

**วิธีตรวจสอบว่าสำเร็จ:**
```bash
# ตรวจสอบว่ามีไฟล์ใหม่
ls -la node_modules/.prisma/client/

# หรือเปิดไฟล์ PrismaV1Adapter.js แล้วลอง autocomplete
# พิมพ์: prisma.users.update({ data: { must...
# ควรมี mustChangePassword ใน autocomplete
```

---

### ⚙️ ขั้นตอนที่ 2: ตั้งค่า Environment Variables

**ทำที่:** `backend/api-server/.env`

#### สำหรับ Development (Local):

```env
# Email Service (ใช้ Email API Backend)
EMAIL_API_URL=http://localhost:3001
EMAIL_API_KEY=your-secret-api-key-here

# Frontend URL (ใช้ใน email link)
FRONTEND_URL=http://localhost:5173

# เปิดใช้งาน email notifications
ENABLE_EMAIL_NOTIFICATIONS=true
```

#### สำหรับ Production (Supabase/Real):

```env
# ถ้าใช้ SendGrid
EMAIL_API_URL=https://api.sendgrid.com/v3/mail/send
EMAIL_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ถ้าใช้ Mailgun
EMAIL_API_URL=https://api.mailgun.net/v3/YOUR_DOMAIN/messages
EMAIL_API_KEY=key-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Frontend URL (production)
FRONTEND_URL=https://dj-system.example.com

# เปิดใช้งาน email notifications
ENABLE_EMAIL_NOTIFICATIONS=true
```

**ตัวอย่างไฟล์ .env สมบูรณ์:**
```env
# Server
NODE_ENV=development
PORT=3000

# Database (Supabase)
DATABASE_URL=postgresql://postgres.xxx:password@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# JWT
JWT_SECRET=your-super-secret-key-change-me-in-production-12345

# Email Service ⭐ เพิ่มใหม่
EMAIL_API_URL=http://localhost:3001
EMAIL_API_KEY=your-secret-api-key-here
FRONTEND_URL=http://localhost:5173
ENABLE_EMAIL_NOTIFICATIONS=true

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
```

---

## 🚀 ขั้นตอน Deploy (ทำตามลำดับ)

### 1. เตรียม Backend (API Server)

1.  **SSH เข้า Server**: Access ไปยังเครื่องที่จะ Deploy
2.  **ดึง Code ล่าสุด**: `git pull origin main`
3.  **ตรวจสอบ Prisma Version (สำคัญ!)**:
    - ต้องใช้ **Prisma v5.22.0** เท่านั้น
    - ห้ามใช้ v7+ เนื่องจากไม่รองรับ `url` ใน schema
    - ตรวจสอบ: `npx prisma -v`
4.  **Install Application Dependencies**:
    ```bash
    cd backend/api-server
    npm install
    # ตรวจสอบว่าได้ Prisma v5.22.0 หรือไม่
    npm list prisma
    ```

### 2. เริ่ม Backend Server
```bash
cd backend/api-server
npm run dev

# ควรเห็น
# Server running on port 3000
# Database connected
```

### 3. เริ่ม Frontend Server
```bash
cd frontend
npm run dev

# ควรเห็น
# Local: http://localhost:5173
```

### 3. ทดสอบระบบ End-to-End

#### Test 1: ลงทะเบียนผู้ใช้
```
1. เปิด http://localhost:5173/register
2. กรอกข้อมูล (ชื่อ, นามสกุล, อีเมล, โทร, แผนก)
3. ⚠️ ห้ามมีช่อง password
4. กดส่งคำขอ
5. ✅ ควรเห็นข้อความ "ส่งคำขอสำเร็จ"
```

#### Test 2: Admin อนุมัติ
```
1. Login เป็น Admin
2. ไปหน้า "Pending Approvals"
3. เลือก user ที่รอการอนุมัติ
4. กดอนุมัติ
5. ✅ ควรเห็น:
   - Email sent successfully (ถ้าส่งเมล์สำเร็จ)
   - หรือแสดง password พร้อม copy button (ถ้าส่งเมล์ไม่สำเร็จ)
```

#### Test 3: Login ด้วย Temp Password
```
1. ไปหน้า http://localhost:5173/login
2. ใส่ email และ temp password ที่ได้จาก email (หรือจาก modal)
3. กด Login
4. ✅ ควรถูก redirect ไปหน้า /force-change-password โดยอัตโนมัติ
```

#### Test 4: เปลี่ยน Password
```
1. ใส่ password ใหม่ (ต้องมี 8+ ตัว, มีตัวพิมพ์ใหญ่, ตัวเล็ก, ตัวเลข, อักขระพิเศษ)
2. ใส่ confirm password
3. กดยืนยัน
4. ✅ ควรถูก redirect ไปหน้า dashboard
```

#### Test 5: Login ด้วย Password ใหม่
```
1. Logout
2. Login ด้วย password ใหม่
3. ✅ ควรเข้าสู่ระบบได้ปกติโดยไม่ต้องเปลี่ยน password อีก
```

---

## 🧪 Test Cases ทั้งหมด

### ✅ Happy Path
- [ ] ลงทะเบียนได้ (ไม่ต้องใส่ password)
- [ ] Admin อนุมัติได้
- [ ] Email ส่งสำเร็จ
- [ ] User login ด้วย temp password ได้
- [ ] ระบบบังคับเปลี่ยน password
- [ ] User ตั้ง password ใหม่ได้
- [ ] Login ด้วย password ใหม่ได้
- [ ] เข้า dashboard ได้

### ⚠️ Error Cases
- [ ] Email ซ้ำ → แสดง error "อีเมลนี้ถูกใช้งานแล้ว"
- [ ] Email ส่งไม่สำเร็จ → แสดง password ใน modal
- [ ] Password อ่อน → แสดง error "รหัสผ่านไม่ปลอดภัย"
- [ ] Password ไม่ตรงกัน → แสดง error "รหัสผ่านไม่ตรงกัน"
- [ ] Login ด้วย password ผิด → แสดง error

### 🔒 Security Tests
- [ ] ไม่สามารถอนุมัติตัวเองได้ (ต้องเป็น admin)
- [ ] ไม่สามารถเปลี่ยน password คนอื่นได้
- [ ] Token หมดอายุแล้ว login ใหม่ได้
- [ ] Password ถูก hash ในฐานข้อมูล (ไม่เป็น plain text)

---

## 📊 ตรวจสอบข้อมูลในฐานข้อมูล

### ตรวจสอบว่า Column ถูกสร้างแล้ว
```sql
-- Supabase SQL Editor
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_name = 'users'
  AND column_name = 'must_change_password';

-- ควรได้ผลลัพธ์:
-- column_name         | data_type | is_nullable
-- must_change_password| boolean   | YES
```

### ตรวจสอบ User ที่ถูกอนุมัติ
```sql
-- ดู user ที่ต้องเปลี่ยน password
SELECT id, email, first_name, last_name,
       registration_status, must_change_password, is_active
FROM users
WHERE must_change_password = true;
```

### ตรวจสอบ Password ถูก Hash
```sql
-- ดู password hash (ควรเป็น bcrypt hash)
SELECT id, email,
       LEFT(password_hash, 7) as hash_prefix
FROM users
WHERE id = 123;

-- hash_prefix ควรขึ้นต้นด้วย: $2b$10$
```

---

## 🔄 Workflow ทั้งระบบ

```
┌─────────────────────────────────────────────────────────────┐
│ 1. USER REGISTRATION (ไม่ต้องใส่ password)                  │
├─────────────────────────────────────────────────────────────┤
│ • Fill form: ชื่อ, นามสกุล, email, โทร, แผนก                │
│ • Submit → POST /api/v2/auth/register-request              │
│ • Response: "ส่งคำขอสำเร็จ รอการอนุมัติ"                     │
│ • Database: registration_status = PENDING                   │
│             is_active = false                               │
│             password_hash = '' (ว่าง)                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. ADMIN APPROVAL                                           │
├─────────────────────────────────────────────────────────────┤
│ • Admin เปิด Pending Approvals panel                        │
│ • เลือก user → กด "อนุมัติ"                                 │
│ • Backend:                                                  │
│   1. Generate password (12 chars)                          │
│   2. Hash password (bcrypt)                                │
│   3. Update database:                                      │
│      - registration_status = APPROVED                      │
│      - is_active = true                                    │
│      - must_change_password = true ⭐                       │
│      - password_hash = <bcrypt hash>                       │
│   4. Send email to user                                    │
│ • Email contains:                                          │
│   - Temporary password                                     │
│   - Login URL                                              │
│   - Warning about forced password change                  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. USER RECEIVES EMAIL                                      │
├─────────────────────────────────────────────────────────────┤
│ Subject: ✅ บัญชีของคุณได้รับการอนุมัติแล้ว                 │
│                                                             │
│ เนื้อหา:                                                    │
│ ┌──────────────────────────────────────┐                   │
│ │ รหัสผ่านชั่วคราว: aB3@mNpQxYz      │                   │
│ └──────────────────────────────────────┘                   │
│                                                             │
│ ⚠️ คำเตือน:                                                 │
│ - ใช้รหัสนี้เพื่อเข้าสู่ระบบครั้งแรก                        │
│ - ระบบจะบังคับให้เปลี่ยนรหัสผ่านใหม่                        │
│                                                             │
│ [ปุ่มเข้าสู่ระบบ]                                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. FIRST LOGIN (ด้วย temp password)                        │
├─────────────────────────────────────────────────────────────┤
│ • User ไปหน้า /login                                        │
│ • ใส่ email + temp password                                │
│ • POST /api/v2/auth/login                                  │
│ • Backend ตรวจสอบ:                                          │
│   - Password ถูกต้องไหม? ✅                                 │
│   - User active ไหม? ✅                                     │
│   - must_change_password = true? ✅                        │
│ • Response:                                                │
│   {                                                        │
│     user: { ..., mustChangePassword: true },              │
│     token: "eyJhbGc..."                                   │
│   }                                                        │
│ • Frontend detect mustChangePassword = true                │
│ • Redirect → /force-change-password ⭐                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. FORCE CHANGE PASSWORD                                    │
├─────────────────────────────────────────────────────────────┤
│ • แสดงหน้า ForceChangePassword.tsx                          │
│ • User ใส่:                                                 │
│   - Password ใหม่ (8+ chars, mixed case, number, special)  │
│   - Confirm password                                       │
│ • Validation:                                              │
│   - Password ยาวพอ? ✅                                       │
│   - มีตัวพิมพ์ใหญ่/เล็ก? ✅                                   │
│   - มีตัวเลข? ✅                                            │
│   - มีอักขระพิเศษ? ✅                                        │
│   - ตรงกันไหม? ✅                                           │
│ • Password strength indicator:                            │
│   - อ่อน (แดง) / ปานกลาง (เหลือง) / แข็งแรง (เขียว)       │
│ • Submit → POST /api/v2/auth/change-password               │
│ • Backend:                                                 │
│   - Hash password ใหม่                                     │
│   - Update: password_hash = <new hash>                     │
│   - Update: must_change_password = false ⭐                │
│ • Redirect → /dashboard                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. NORMAL ACCESS                                            │
├─────────────────────────────────────────────────────────────┤
│ • User เข้าใช้งานระบบได้ปกติ                                │
│ • Login ครั้งต่อไป:                                         │
│   - ใช้ password ใหม่                                       │
│   - ไม่ถูกบังคับเปลี่ยน password อีก                         │
│   - เข้า dashboard ได้เลย                                  │
│ • Database:                                                │
│   - must_change_password = false                           │
│   - is_active = true                                       │
│   - registration_status = APPROVED                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 ความปลอดภัย

### Password Generation
```javascript
// 12 ตัวอักษร, มีทุก category:
- Uppercase: ABCDEFGHJKLMNPQRSTUVWXYZ
- Lowercase: abcdefghjkmnpqrstuvwxyz
- Numbers: 23456789
- Special: @#$%&*

// ตัวอย่าง: aB3@mNpQxYz
```

### Password Hashing
```javascript
// Bcrypt with 10 rounds
const hash = await bcrypt.hash(password, 10);

// ผลลัพธ์: $2b$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy
```

### Email Security
- ✅ ส่งผ่าน HTTPS เท่านั้น
- ✅ ส่งไปที่ email ที่ลงทะเบียนเท่านั้น
- ✅ Password ไม่ถูก log หรือ trace
- ✅ Plain text พาสเวิร์ดแสดงครั้งเดียวใน email

---

## 📝 Checklist สุดท้ายก่อน Go Live

### Backend
- [ ] รัน migration 015 (✅ เสร็จแล้ว)
- [ ] รัน `npx prisma generate`
- [ ] ตั้งค่า EMAIL_API_URL และ EMAIL_API_KEY
- [ ] ตั้งค่า FRONTEND_URL
- [ ] เปิด ENABLE_EMAIL_NOTIFICATIONS=true
- [ ] ทดสอบ API endpoints ทั้งหมด
- [ ] ทดสอบส่งเมล์

### Frontend
- [ ] Route /force-change-password ทำงาน (✅ เสร็จแล้ว)
- [ ] Login redirect ถูกต้อง (✅ เสร็จแล้ว)
- [ ] Register form ไม่มีช่อง password (✅ เสร็จแล้ว)
- [ ] ForceChangePassword component ทำงาน (✅ เสร็จแล้ว)
- [ ] Build production ไม่มี error

### Database
- [ ] Column must_change_password มีอยู่ (✅ เสร็จแล้ว)
- [ ] RLS policies อนุญาตการอ่าน/เขียน column นี้
- [ ] Backup ข้อมูลก่อน deploy

### Email Service
- [ ] มี API key ที่ใช้งานได้
- [ ] Test ส่งเมล์สำเร็จ
- [ ] Email template แสดงผลถูกต้อง
- [ ] ภาษาไทยแสดงผลถูกต้อง

---

## 🎯 สรุป

**สถานะปัจจุบัน:**
- ✅ Code พร้อม 100%
- ✅ Database พร้อม 100%
- ⚠️ Prisma Client ต้อง regenerate
- ⚠️ Environment variables ต้องตั้งค่า

**เมื่อทำ 2 ขั้นตอนสุดท้ายแล้ว:**
- 🚀 พร้อม deploy ได้เลย
- 🧪 ทดสอบ end-to-end ให้ครบ
- 📊 Monitor logs และ metrics

---

## 📞 Support

**เอกสารอ้างอิง:**
- `DEPLOYMENT_REPORT_2026-01-30.md` - รายงานฉบับเต็ม
- `API_ENDPOINTS_SUMMARY.md` - เอกสาร API
- `USER_REGISTRATION_APPROVAL_WORKFLOW.md` - คู่มือ workflow

**ติดปัญหา:**
1. อ่าน Troubleshooting ใน DEPLOYMENT_REPORT_2026-01-30.md
2. ตรวจสอบ console logs (backend + frontend)
3. ตรวจสอบ database ว่า column ถูกสร้าง

---

**สร้างโดย:** Claude Code Agent
**วันที่:** 30 มกราคม 2026
**เวอร์ชัน:** 1.0
**สถานะ:** ✅ Ready for Deployment
