# คู่มือขั้นตอนการลงทะเบียนและอนุมัติผู้ใช้ (V2)
**สถานะ:** เสร็จสมบูรณ์
**วันที่อัปเดต:** 30 มกราคม 2026

---

## 1. ภาพรวม (Overview)

เอกสารฉบับนี้อธิบายกระบวนการรับผู้ใช้เข้าสู่ระบบโดยผ่านการควบคุมและอนุมัติจากผู้ดูแลระบบ (Admin) มีคุณลักษณะเด่นดังนี้:
- **ผู้ใช้ไม่ต้องตั้งรหัสผ่านเอง** ในตอนลงทะเบียน เพื่อความรวดเร็วและเป็นระเบียบ
- **Admin เป็นผู้อนุมัติ** และระบบจะสร้างรหัสผ่านชั่วคราวให้โดยอัตโนมัติ
- **ความปลอดภัยสูง** ผู้ใช้ถูกบังคับให้เปลี่ยนรหัสผ่านใหม่ทันทีเมื่อเข้าใช้งานครั้งแรก

---

## 2. ผังขั้นตอนการทำงาน (Workflow)

1. **ผู้ใช้สมัครใช้งาน**: กรอกข้อมูลพื้นฐานในหน้าลงทะเบียน (ไม่มีช่องรหัสผ่าน)
2. **ระบบบันทึกคำขอ**: ตั้งสถานะเป็น `PENDING` และยังไม่อนุญาตให้ล็อกอิน
3. **Admin ตรวจสอบ**: ผู้ดูแลระบบดูรายการคำขอที่ค้างอยู่ผ่านหน้า Dashboard
4. **การอนุมัติ**: เมื่อ Admin กดอนุมัติ:
   - ระบบสุ่มรหัสผ่านชั่วคราวให้
   - ตั้งค่าให้ผู้ใช้ต้องเปลี่ยนรหัสผ่าน (`mustChangePassword = true`)
   - ส่งอีเมลแจ้งผู้ใช้พร้อมรหัสผ่านชั่วคราว
5. **การเข้าใช้งาน**: 
   - ผู้ใช้รับอีเมลและนำรหัสผ่านชั่วคราวมาล็อกอิน
   - ระบบตรวจพบสถานะบังคับเปลี่ยนรหัสผ่าน จึงพาส่งไปยังหน้าตั้งรหัสผ่านใหม่
   - ผู้ใช้ตั้งรหัสผ่านใหม่และเข้าสู่ระบบได้อย่างสมบูรณ์

---

## 3. การเปลี่ยนแปลงในฐานข้อมูล

มีการเพิ่มคอลัมน์สำคัญในตาราง `users` ดังนี้:
- `status`: เก็บสถานะ (PENDING, APPROVED, REJECTED)
- `registered_at`, `approved_at`: เก็บระยะเวลาดำเนินงาน
- `must_change_password`: ใช้สำหรับบังคับเปลี่ยนรหัสผ่าน
- `rejection_reason`: เก็บเหตุผลกรณีไม่อนุมัติ

---

## 4. ส่วนประกอบในหน้าจอใช้งาน (Frontend)

- **หน้าลงทะเบียน**: ปรับปรุงหน้า `Register.jsx` เดิมให้นำช่องรหัสผ่านออก
- **หน้ายืนยัน**: แจ้งผู้ใช้ว่าได้รับข้อมูลแล้ว และให้รอรับอีเมลแจ้งผล
- **แผงควบคุม Admin**: หน้า `PendingApprovals.tsx` สำหรับจัดการคำขอทั้งหมด
- **หน้าส่งรหัสผ่านใหม่**: หน้า `/force-change-password` สำหรับบังคับเปลี่ยนรหัสผ่าน

---

## 5. การรักษาความปลอดภัย

- **bcrypt**: รหัสผ่านทั้งหมดจะถูกเข้ารหัสก่อนเก็บลงฐานข้อมูลเสมอ
- **Role Control**: เฉพาะ Admin เท่านั้นที่มีสิทธิ์อนุมัติและกำหนดบทบาท (Role) ของผู้ใช้
- **JWT**: ระบบจะออก Token ให้เฉพาะผู้ใช้ที่ผ่านการอนุมัติแล้วเท่านั้น

---

**สรุป:** กระบวนการนี้ช่วยให้องค์กรมั่นใจได้ว่าผู้ที่เข้าถึงระบบคือบุคคลที่ได้รับการตรวจสอบแล้วจริงๆ ช่วยเพิ่มความปลอดภัยและลดภาระของผู้ใช้ในการจำรหัสผ่านในขั้นตอนแรก 🔒
