# สรุปข้อมูล API Endpoints: User Registration Approval Workflow
**วันที่:** 30 มกราคม 2026
**เวอร์ชัน:** 1.0

---

## 1. คำขอลงทะเบียน (ผู้ใช้ - ไม่ต้องระบุรหัสผ่าน)

### Endpoint
`POST /api/v2/auth/register-request`

### รายละเอียด
ผู้ใช้ส่งแบบฟอร์มลงทะเบียนโดยไม่ต้องกรอกรหัสผ่าน ข้อมูลจะถูกบันทึกในสถานะ `PENDING`

### ตัวอย่างข้อมูลฝั่งชื่อ (Request Body)
```json
{
  "email": "user@example.com",
  "firstName": "สมเด็จ",
  "lastName": "ศรี",
  "tenantId": 1
}
```

### การตอบกลับ (สำเร็จ)
```json
{
  "success": true,
  "message": "ส่งคำขอลงทะเบียนเรียบร้อยแล้ว กรุณารอการอนุมัติจากผู้ดูแลระบบ"
}
```

---

## 2. การอนุมัติการลงทะเบียน (ผู้ดูแลระบบ)

### Endpoint
`POST /api/v2/auth/approve-registration`

### รายละเอียด
ผู้ดูแลระบบอนุมัติการสมัคร ระบบจะสร้างรหัสผ่านชั่วคราวและส่งอีเมลแจ้งผู้ใช้โดยอัตโนมัติ

### การยืนยันตัวตน
ต้องใช้: Bearer token (เฉพาะสิทธิ์ผู้ดูแลระบบเท่านั้น)

### ตัวอย่างข้อมูล (Request Body)
```json
{
  "userId": 123,
  "approvedById": 456,
  "roleName": "Member"
}
```

### การตอบกลับ (สำเร็จ)
- `temporaryPassword`: รหัสผ่านลับที่ถูกสร้างขึ้น (ใช้สำรองกรณีอีเมลส่งไม่สำเร็จ)
- `emailSent`: แจ้งให้ทราบว่าอีเมลส่งสำเร็จหรือไม่ (`true`/`false`)

---

## 3. การปฏิเสธการลงทะเบียน (ผู้ดูแลระบบ)

### Endpoint
`POST /api/v2/auth/reject-registration`

### รายละเอียด
ผู้ดูแลระบบปฏิเสธคำขอลงทะเบียน และระบบจะส่งอีเมลแจ้งผลให้ผู้ใช้ทราบ

---

## 4. การเปลี่ยนรหัสผ่าน (ผู้ใช้ - บันทึกรหัสผ่านใหม่)

### Endpoint
`POST /api/v2/auth/change-password`

### รายละเอียด
ผู้ใช้ตั้งรหัสผ่านใหม่หลังจากล็อกอินด้วยรหัสผ่านชั่วคราวครั้งแรก เพื่อล้างสถานะ `mustChangePassword`

### ข้อกำหนดของรหัสผ่าน
- ความยาวอย่างน้อย 8 ตัวอักษร
- ต้องประกอบด้วยอักษรตัวเล็ก, ตัวใหญ่, ตัวเลข และสัญลักษณ์พิเศษ

---

## 5. การเข้าสู่ระบบ (ล็อกอิน)

### Endpoint
`POST /api/v2/auth/login`

### รายละเอียดการตอบกลับที่สำคัญ
หากผู้ใช้เพิ่งได้รับการอนุมัติ ค่าในฟิลด์ `mustChangePassword` จะเป็น `true` ซึ่งหน้าบ้าน (Frontend) จะนำผู้ใช้ไปยังหน้าเปลี่ยนรหัสผ่านทันที

---

## รหัสข้อผิดพลาด (Error Codes)

| รหัส | คำอธิบาย |
|------|-------------|
| `EMAIL_EXISTS` | อีเมลนี้ถูกลงทะเบียนไว้ในระบบแล้ว |
| `USER_NOT_FOUND` | ไม่พบข้อมูลผู้ใช้ในระบบ |
| `WEAK_PASSWORD` | รหัสผ่านไม่ตรงตามมาตรฐานความปลอดภัย |
| `ACCOUNT_NOT_ACTIVE` | บัญชียังไม่ได้รับการอนุมัติหรือถูกระงับ |

---

**สรุป:** API ชุดนี้ได้รับการออกแบบมาเพื่อรองรับขั้นตอนการตรวจสอบความถูกต้องของผู้ใช้ก่อนเข้าถึงระบบ เพื่อความปลอดภัยสูงสุดของข้อมูล 🛡️
