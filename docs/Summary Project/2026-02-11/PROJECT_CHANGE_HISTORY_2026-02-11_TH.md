# 📝 บันทึกการเปลี่ยนแปลงความต้องการ (Project Change History)

เอกสารฉบับนี้บันทึก "จุดเปลี่ยน" สำคัญของโครงการ (Requirement Changes) ที่เกิดขึ้นระหว่างการพัฒนา เพื่อใช้ในการติดตามย้อนกลับ (Traceability) และอธิบายเหตุผลของการปรับปรุงระบบ

---

## 🔄 รายการการเปลี่ยนแปลง (Change Logs)

### **Change #1: Unification of Database Tables**
- **วันที่:** 19 มกราคม 2569
- **จากเดิม:** มีการแยกตาราง `v2_users` และ `v2_organizations` ออกมาจากระบบเดิม
- **เปลี่ยนเป็น:** ยุบรวมการใช้งานมาที่ตารางชุดหลัก (V1) ทั้งหมด เพื่อป้องกันข้อมูลซ้ำซ้อน (Single Source of Truth)

### **Change #2: Transition to Modular Architecture**
- **วันที่:** 23 มกราคม 2569
- **จากเดิม:** โครงสร้างโค้ดแบบเดิมที่ทุกอย่างอยู่ในโฟลเดอร์เดียว
- **เปลี่ยนเป็น:** จัดระเบียบใหม่เป็น **Modular Architecture (Plugin Style)** เพื่อให้สามารถเพิ่ม/ลบฟีเจอร์ได้โดยไม่กระทบระบบส่วนกลาง

### **Change #3: Security & REST API Migration**
- **วันที่:** 28 มกราคม 2569
- **จากเดิม:** Frontend เรียกใช้งานฐานข้อมูล Supabase โดยตรง (Direct Query)
- **เปลี่ยนเป็น:** พัฒนา Backend API Server มาคั่นกลางเพื่อให้สามารถควบคุม Business Logic และเพิ่มความปลอดภัย (Security Layer)

### **Change #4: Permission Scope Refactoring**
- **วันที่:** 4 กุมภาพันธ์ 2569
- **จากเดิม:** การตรวจสอบสิทธิ์อิงตาม `Tenant Scope` ซึ่งซ้ำซ้อนกับข้อมูลบริษัท
- **เปลี่ยนเป็น:** ปรับเป็น `Company Scope` และ `BUD Scope` เพื่อความชัดเจนในการเข้าถึงข้อมูลโครงการ

### **Change #5: Role Naming Standardization**
- **วันที่:** 9 กุมภาพันธ์ 2569
- **จากเดิม:** เรียกชื่อบทบาทไม่สม่ำเสมอ (เช่น OrgAdmin, Marketing, User)
- **เปลี่ยนเป็น:** กำหนดมาตรฐานใหม่เป็น **Requester, Approver, Assignee, Admin** เพื่อความเข้าใจที่ตรงกันในทุกหน้าจอ

### **Change #6: Full System Integration (V2 Auth to V1 DB)**
- **วันที่:** 10 กุมภาพันธ์ 2569
- **จากเดิม:** ระบบยืนยันตัวตน V2 ตัดขาดจากข้อมูล V1 ทำให้ login ไม่ได้
- **เปลี่ยนเป็น:** พัฒนา Adapter Layer เพื่อให้หน้าจอ V2 สามารถใช้งานร่วมกับข้อมูลจริงใน V1 ได้อย่างไร้รอยต่อ

### **Change #7: Sequential Job Chaining (Phase 2) + Cascade Reject**
- **วันที่:** 11 กุมภาพันธ์ 2569
- **จากเดิม:**
  - งานผูกกัน (nextJobTypeId) สร้างได้ทีละระดับเดียว (Single-Level)
  - การปฏิเสธงานแม่ส่งแค่ notification แต่ลูกยังค้างเป็น "pending"
- **เปลี่ยนเป็น:**
  - **Limited Full-Chain (MAX_DEPTH=3):** สร้างงาน A จะสร้าง B และ C มาเลยอัตโนมัติ (A→B→C)
  - **Urgent Job Reschedule:** งานด่วนแทรก ขยับงานคนอื่น +2 วันทำการ
  - **Fixed Schedule / Flexible Start:** วันส่งมอบคงที่ แต่เริ่มทำได้เลยถ้างานก่อนเสร็จ
  - **Cascade Reject Downstream:** ปฏิเสธงานใดงานหนึ่ง งานต่อเนื่องทั้งหมดถูกปฏิเสธตาม
- **ไฟล์ที่เพิ่ม/แก้ไข:**
  - `backend/api-server/src/config/chainConfig.js` - Configuration
  - `backend/api-server/src/services/chainService.js` - Business Logic
  - `backend/api-server/src/services/approvalService.js` - Cascade Reject Integration
  - `backend/api-server/src/routes/jobs.js` - Route Integration
  - `docs/03-features/JOB-CHAINING-GUIDE.md` - Documentation

---

## 💡 เหตุผลเบื้องหลังจุดเปลี่ยน
ส่วนใหญ่เกิดจากการตรวจสอบทางเทคนิค (Technical Audit) และการรับฟังความต้องการเพิ่มเติมจากผู้ใช้จริง เพื่อทำให้ระบบมีความ:
1. **Scalability:** รองรับโครงการจำนวนมากได้ในอนาคต
2. **Security:** ข้อมูลมีความปลอดภัยตามมาตรฐานสากล
3. **Consistency:** ประสบการณ์การใช้งาน (UX) ราบรื่นและลดความสับสนเรื่องชื่อเรียกบทบาทต่างๆ

---
*บันทึกข้อมูลล่าสุดโดย: Antigravity AI Assistant*
