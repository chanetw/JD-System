# 📋 DJ System - Software Requirements Specification (SRS)

## 1. Introduction
ระบบ **DJ System (Design Job Management)** คือระบบบริหารจัดการใบงานออกแบบสำหรับ Sena Development เพื่อลดขั้นตอนการส่งงานทาง LINE/Email และสามารถติดตามสถานะงาน (SLA) วัดผล KPI ได้อย่างมีประสิทธิภาพ

## 2. User Roles & Permissions
| Role | รายละเอียด | สิทธิ์การใช้งาน |
|------|-----------|----------------|
| **Requester** | ผู้ขอรับบริการ (Marketing, Sales) | - สร้างใบงาน (Create Job)<br>- ติดตามสถานะ (My Jobs)<br>- ให้ Feedback/Approve งาน |
| **Approver** | ผู้อนุมัติงาน (Manager/Director) | - อนุมัติการขอจ้าง (Approve Request)<br>- ตรวจสอบงบประมาณ/รายละเอียด |
| **Assignee** | ผู้รับผิดชอบงาน (Graphic/Editor) | - รับงาน (Acknowledge)<br>- อัปเดตความคืบหน้า (Update Status)<br>- ส่งไฟล์งาน (Deliverables) |
| **Admin** | ผู้ดูแลระบบ | - จัดการ Users, Organization (BU/Dept)<br>- ตั้งค่า Job Types & SLA |

## 3. Functional Requirements

### 3.1 Authentication & Authorization
- **Dual Login System:**
  1. **Production Login (`/login`):** รองรับการ Login ด้วย Email/Password จริง (ผ่าน Supabase Auth)
  2. **Demo Login (`/login_demo`):** หน้าจอสำหรับเลือก User จาก Dropdown เพื่อทดสอบระบบ (Mock Users)
- อนาคตต้องรองรับการเชื่อมต่อ AD/LDAP (Phase 3)
- User 1 คนสามารถมีหลาย Role ได้ (เช่น Marketing + Approver)

### 3.2 Job Request Management (Phase 2 Core)
- **Create Job:**
  - เลือก Project, Job Type ได้
  - กรอกรายละเอียด Brief (Headline, Objective, Size, Format)
  - แนบไฟล์ Reference ได้
- **Validation:**
  - ห้ามส่งงานถ้าข้อมูลไม่ครบตาม Job Type Config
  - แจ้งเตือน SLA ล่วงหน้าตามประเภทงาน

### 3.4 Auto-Start & Job Execution (Phase 4)
- **Concept:** งานจะเริ่มนับ SLA (`started_at`) ทันทีที่ Assignee "รับรู้" งานน้ัน
- **Triggers:**
  1. **Immediate Start:** เมื่อ Assignee เปิดหน้า Job Detail เป็นครั้งแรก (View Event)
  2. **Auto-Start Timeout:** หาก Assignee ไม่เปิดดูภายในเวลาที่กำหนด (Default: 4 ชม.) ระบบจะ Force Start อัตโนมัติ
- **Configuration:**
  - กำหนดค่า `auto_start_hours` ได้ที่ระดับ **Job Type** (เช่น งานด่วน = 1 ชม., งานปกติ = 4 ชม.)
  - หาก Job Type ไม่ได้ระบุ ให้ใช้ค่า Default ของ System (4 ชม.)

### 3.3 Job Status Workflow
Sequence ของสถานะงาน:
`Draft` -> `Pending Approval` -> `Approved` -> `Assigned` (Waiting for Start) -> `In Progress` -> `Review` -> `Completed` -> `Closed`

### 3.5 My Queue / Inbox (Phase 5)
- **Objective:** หน้า Dashboard ส่วนตัวสำหรับ Assignee เพื่อจัดการงานในมือให้มีประสิทธิภาพ
- **Structure (Tab Breakdown):**
  1. **📋 To Do:** งานใหม่ที่ได้รับมอบหมาย (Status: `Assigned`) *ยังไม่ได้กด Start*
  2. **⚡ In Progress:** งานที่กำลังทำอยู่ (Status: `In Progress`)
  3. **⏳ Waiting:** งานที่รอ Action จากผู้อื่น (Status: `Correction` ที่รอตรวจ, `Pending Approval`)
  4. **✅ Done:** ประวัติงานที่ทำเสร็จแล้ว (Status: `Completed`, `Closed`)
- **Job Health Indicators (SLA Coloring):**
  - 🟢 **Normal:** เหลือเวลา > 48 ชั่วโมง
  - 🟡 **Warning:** เหลือเวลา <= 48 ชั่วโมง
  - 🔴 **Critical/Overdue:** เลยกำหนด หรือ เหลือเวลา < 4 ชั่วโมง

## 5. Multi-tenancy & Data Scoping
เพื่อให้ระบบรองรับการทำงานแบบหลายบริษัท (Multi-tenant) และมีการจัดลำดับความสำคัญของข้อมูลที่ถูกต้อง

### 5.1 Hierarchical Logic
- **Tenant (ระดับบริษัท):** แยกข้อมูลขาดจากกัน (Isolation) เช่น SENA และ SENX
- **Department (ระดับแผนก):** อยู่ภายใต้ Tenant โดย 1 แผนกมี 1 Manager
- **Project (ระดับโครงการ):** ผูกกับ Tenant และสามารถระบุแผนกที่รับผิดชอบได้
- **Job (ใบงาน):** ผูกกับ Project โดยอัตโนมัติตาม Tenant ของ Project นั้น

### 5.2 Data Visibility Rules
- **Users:** มองเห็นเฉพาะข้อมูลภายใต้ Tenant ของตัวเองเท่านั้น
- **Requester:**
    - เห็นเฉพาะโครงการ (Project) ของ Tenant ตัวเอง
    - เมื่อสร้าง Job ระบบจะดึง Manager ของแผนกที่ Requester สังกัดมาเป็นผู้ตรวจสอบ (Fallback)
- **Assignee:**
    - มองเห็น Job ที่ได้รับมอบหมาย (แม้จะเป็นการจ้างข้าม Tenant)
- **Admin:**
    - **Tenant Admin:** จัดการได้เฉพาะใน Tenant ตัวเอง
    - **Super Admin:** จัดการได้ทุก Tenant (ใช้สำหรับการ Setup เริ่มต้น)

### 5.3 Manager & Team Lead Logic
- การระบุผู้อนุมัติ (Approver) จะอิงตาม **Approval Flow** ของโครงการนั้นๆ
- หากโครงการไม่มีการตั้งค่าเฉพาะ: ระบบจะใช้ **Department Manager** ของผู้ขอ (Requester) เป็นคนแรกเสมอ
- ความสัมพันธ์ต้องตรวจสอบ: `User -> Department -> Manager` (ห้ามมีการวนลูปหรือ Manager ว่าง)

---

## 6. Data Needs (For SA)
- **Tenant:** ID, Name, Domain
- **Department:** ID, TenantID, ManagerID (User), Name
- **Project:** ID, TenantID, DepartmentID (Optional), Name, Status
- **User:** ID, TenantID, DepartmentID, Role
- **Job Data:** รหัสเอกสาร (DJ-YYYY-XXXX), ผู้ขอ, ผู้ทำ, วันกำหนดส่ง, สถานะ, ProjectID
- **Log:** ประวัติการแก้ไข, เวลาที่ใช้ในแต่ละขั้นตอน (SLA Tracking), IP Address
